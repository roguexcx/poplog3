import type { IcsSeriesGroup } from "@/lib/ics-engine";

export async function applyRetrofill(
  _groups: IcsSeriesGroup[],
  _accessToken: string,
): Promise<void> {
  // TMDB retrofill permanently disabled
}
