/**
 * @deprecated Use `@/components/ui/ActionButton` em vez disto. O
 * `ActionButton` cobre variantes primary/secondary/ghost/danger, suporta
 * `active`, `loading`, `leftIcon`/`rightIcon` e e o botao canonico do
 * design system. Mantido apenas como ponte temporaria.
 */
export {};
