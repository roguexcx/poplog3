/**
 * @deprecated Importe de `@/components/ui/SectionHeader` em vez deste caminho.
 * Este arquivo permanece apenas como ponte temporaria e sera removido.
 */
export { default } from "@/components/ui/SectionHeader";
